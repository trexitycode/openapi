lang: Shell
source: |
  curl -X PATCH {server-url}/shipments/TRX973036599-6KL\
    -H 'Content-Type: application/json'\
    -H 'Authorization: Bearer <Trexity API Key>'\
    -d '{
      "pickupNotes": "Needs to go out ASAP!"
    }'
