lang: Shell
source: |
  curl -X PATCH {server-url}/shipments/TRX1132313511829/parcels/0\
    -H 'Content-Type: application/json'\
    -H 'Authorization: Bearer <Trexity API Key>'\
    -d '{
      "base64image": "(base64 encoded image here, truncated)"
    }'
