description: Payload describing a parcel's photo
type: object
required:
  - shipmentId
  - index
properties:
  shipmentId:
    type: string
  index:
    type: number
    minimum: 0
    description: The index of the parcel within the shipment to upload the photo for
  base64Image:
    type: string
    description: Base64 encoded image data