description: A registered webhook
type: object
required:
  - id
  - createdAt
  - merchantId
  - type
  - eventName
  - url
properties:
  id:
    type: string
  createdAt:
    type: string
    format: date-time
  merchantId:
    type: string
  type:
    type: string
    enum: [merchant-webhook]
  eventName:
    $ref: ./WebhookEventName.yaml
  url:
    type: string
    format: uri
