description: Payload object used to update a shipment's parcel
allOf:
  - type: object
    required:
      - id
    properties:
        id:
          type: string
          description: The unique ID (UUID) of the delivery
  - $ref: ../payloads/DeliveryPayload.yaml
