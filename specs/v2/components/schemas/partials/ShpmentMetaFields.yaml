type: object
properties:
  metaField1:
    type: string
    maxLength: 1024
    description: |
      Optional metadata field to store arbitrary data
  metaField2:
    type: string
    maxLength: 1024
    description: |
      Optional metadata field to store arbitrary data
  metaField3:
    type: string
    maxLength: 1024
    description: |
      Optional metadata field to store arbitrary data