description: Rate Limit Exceeded
content:
  application/json:
    schema:
      type: object
      properties:
        errors:
          type: array
          items:
            $ref: '../schemas/Error.yaml'