get:
  operationId: getBatchingOptimizationCreateDeliveriesOperation
  tags: [Batching]
  summary: Get "create deliveries" operation
  description: |
    Get the "create deliveries" operation.

  parameters:
    - name: batchingOperationId
      in: path
      required: true
      schema:
        type: string
    - name: batchingCreateDeliveriesOperationId
      in: path
      required: true
      schema:
        type: string

  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: ../../components/schemas/BatchingCreateDeliveriesOperation.yaml
    '400':
      $ref: ../../components/responses/400.yaml
    '401':
      $ref: ../../components/responses/401.yaml