get:
  operationId: getBatchRateAcceptRequest
  tags: [Batching]
  summary: Get batch rate accept request
  description: |
    Get the status of a rate accept request

  parameters:
    - in: path
      name: batchRateId
      required: true
      schema:
        type: string
      description: The ID of the batch rate.
    - in: path
      name: batchRateAcceptRequestId
      required: true
      schema:
        type: string
      description: The ID of the request to accept the batch rate.

  responses:
    '200':
      description: The status of the batch rate accept request
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: ../../components/schemas/BatchRateAcceptRequest.yaml
    '404':
      $ref: ../../components/error-responses/404.yaml
    '500':
      $ref: ../../components/error-responses/500.yaml
